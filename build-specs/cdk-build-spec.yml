version: 0.2

phases:
  install:
    command:
      -npm install -g npm
      - npm install
    build: 
      command:
        - npm run build
        - npm run cdk --synth

artifacts:
  base-directory: cdk.out
  files:
    - '**/*'